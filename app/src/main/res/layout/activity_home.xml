<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/baby_pink"
    tools:context=".HomeActivity">

    <!-- Home Image -->
    <ImageView
        android:id="@+id/homeImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="48dp"
        android:src="@drawable/home_icon"
        android:contentDescription="Home Icon"
        android:scaleType="centerInside"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/skincareRoutineImage" />

    <!-- Skincare Routine Image -->
    <ImageView
        android:id="@+id/skincareRoutineImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        android:src="@drawable/skincare_routine_icon"
        android:contentDescription="Skincare Routine Icon"
        android:scaleType="centerInside"
        app:layout_constraintTop_toBottomOf="@id/homeImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/skincareShopsImage" />

    <!-- Skincare Shops Image -->
    <ImageView
        android:id="@+id/skincareShopsImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        android:src="@drawable/skincare_shops_icon"
        android:contentDescription="Skincare Shops Icon"
        android:scaleType="centerInside"
        app:layout_constraintTop_toBottomOf="@id/skincareRoutineImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="48dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
